# scRNA-seq pipeline — CPU-only requirements
#
# Used by the CPU Docker image (Phases 00, 02, 04, 06, 07, 08).
# Excludes GPU packages: torch, cellbender, scvi-tools.

# Core single-cell
scanpy>=1.10
anndata>=0.10
scirpy>=0.18
celltypist>=1.6
leidenalg>=0.10

# QC & integration
scrublet>=0.2.3
harmonypy>=0.0.9
scib>=1.1

# Differential expression & pathway analysis
pydeseq2>=0.4
decoupler>=1.6

# Scientific stack
numpy>=1.26
pandas>=2.1
scipy>=1.12
scikit-learn>=1.3

# Plotting
matplotlib>=3.8
seaborn>=0.13

# Utilities
pyyaml>=6.0
